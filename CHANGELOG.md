# 변경 기록

날짜는 작업 시점 기준입니다.

## 2026-09-26

### 체류 시간 산정
- `체류시간 산정/` 폴더 추가 — 플래너의 체류 · 이동 · 일자 창 · 배정 · 자동 코스 로직을 분리한 `stay_schedule.js`, 장소별 체류 시간 CSV, 분류별 요약, 경주 2박 3일 예시 일정, 워드 설명서
- 전국 목록 조립 시 경주 · 거제 장소가 중복 결합돼 id 62개가 겹치는 문제 확인 (문서에 기록, 코드는 미수정)

## 2026-09-25

### 외부 API 연계
- `shared.js` 에 기상청 단기예보(`getKmaForecast`) · 에어코리아 미세먼지(`getAirQuality`) · 한국관광공사 축제·행사(`getFestivals`) · TMDB 작품 정보(`getTitleMeta`) 함수 추가. 전부 `config.js` 키가 비어 있으면 조용히 건너뜀
- 날씨: 공공데이터포털 키가 있으면 오늘·내일을 기상청 단기예보로 덮어쓰고, 어제는 Open-Meteo 유지
- 화면 6개(플래너 + 테마 5종) 장소 상세의 날씨 칸 아래에 미세먼지 줄 추가 (PM10 · PM2.5 · 등급, 5개 언어)
- 플래너 코스 탭에 "여행 기간 행사 · 축제" 목록 추가 — 선택 날짜와 겹치고 코스(없으면 도시 중심) 30km 안 행사, 가까운 순 12개
- 테마 화면 4종(왕과 사는 남자 · 케데헌 · 제주 K-Drama · 부산 영화 기행) 채널 탭 상단에 TMDB 작품 카드 추가. RESCENE 은 작품이 아니라 제외
- `config.example.js` 에 `tmdb` 항목 추가, APIS.md 갱신

## 2026-09-24

### 정리
- `Mobile app design.zip`(17.6MB · 저장소 파일과 중복) · `github/md`(옛 소유자 동기화 메모) 삭제
- 플래너 + 테마 5종 화면에 각각 복제돼 있던 `shared.js` 상수 · 함수 16개를 화면에서 제거하고, 각 화면 `<head>` 에서 `config.js` → `shared.js` 를 읽도록 연결 (화면당 약 120줄 감소)
- 화면에 하드코딩돼 있던 API 키(Google · Kakao · 공공데이터포털 · 한국도로공사)를 `config.js` 로 이동, `config.example.js` 를 코드가 읽는 항목만 남김, 쓰지 않던 `KRIC` 상수 삭제, 루트 `.gitignore` 추가
- README 의 데이터 · 구조 · 디자인 절(각 문서와 중복)을 문서 목록으로 대체, STYLES 이력 절(CHANGELOG 와 중복) 제거, APIS 키 설정 안내를 `config.js` 기준으로 교체, `docs/` 경로 · 옛 파일명 참조 수정, I18N-TODO 줄 번호 재기준

### 문서
- 9-22 에 삭제된 `Tour Navigator (standalone).html` 참조를 README · ARCHITECTURE 에서 제거

### 테마 추천
- `테마 추천 알고리즘/` 폴더 추가 — 선호 문항 14개 → 여행자 군집 10개 → 추천 테마 계산 파이프라인(`run_all.py`), 국민여행조사·외래관광객조사 근거표, 장소 재분류 초안, 설계 문서 3종

## 2026-09-23

### 스타일
- 7개 화면에 중복돼 있던 `<style>` 블록을 `shared.css` 로 추출, 각 화면은 `<link rel="stylesheet" href="shared.css">` 로 링크
- 화면 고유 `@keyframes` (홈 5개 · 플래너 3개)만 각 화면 `<style>` 에 유지
- 홈 화면의 짝이 맞지 않던 `}` 제거, 저장소에 없는 `_ds/…/styles.css` · `_ds_bundle.js` 참조(404) 제거
- `STYLES.md` 추가, ARCHITECTURE · CONTRIBUTING · README 에 반영

## 2026-09-21

### 저장소 정리
- 파일명을 공백 없는 kebab-case로 변경, 내부 링크 일괄 반영
- API 키를 `config.js` 한 파일로 분리하고 `.gitignore` 처리 (`config.example.js` 템플릿 추가)
- 각 화면 상단에 주석 헤더 추가
- Apache-2.0 라이선스, `docs/` 문서 4종, CHANGELOG · ROADMAP 추가

## 2026-09-20

### 데이터
- 전국 장소 1,197곳 / 110여 개 도시로 확대 (열린관광지 목록 40개 도시 추가)
- 김해 관광지 추가
- 전국 데이터 중복 정리

### 기능
- 한국관광공사 TourAPI 연동 — 관광정보 · 사진 · 오디오 가이드 · 스토리텔링, 5개 언어 서비스
- 한국관광 100선(🏅) · 열린관광지 무장애(♿) 배지와 카테고리 필터
- 지역 / 도시 / 유네스코 / 100선 필터를 카테고리 칩으로 통일
- 수도권 전철권 17개 도시 간 전철 기준 경로, 출발역 선택기 분리
- 국내선 9개 공항 · 여객선(제주 · 울릉) · 도시철도(서울 · 부산 · 인천) 경로 추가
- 제주 · 부산 항공 요약 (하드코딩, API 슬롯 확보)
- 투어 목적지 선택기를 전 도시 가나다순 드롭다운으로 변경

### 다국어
- Français 추가 (한 · EN · 中 · 日 · ES · FR)
- 장소명 · 운영시간 · 카테고리 · 요금을 공사 다국어 DB의 공식 표기로 대체, 좌표 기준 조회
- 표기 대체 순서: 선택 언어 DB → 영어 DB → 내장 한국어
- 다국어 전환을 호버 메뉴(MULTI / LANGUAGE)로 정리

### 성능 · UX
- 시계 로딩 화면으로 교체 (핀 팝업 제거)
- 초기 비필수 작업 지연 실행, 공사 DB 적응형 선인출 최대 120건
- 플래너에서 영상 통계 호출 제거, 영상 채널 지연 로드

## 이전

- 홈 화면 · 테마 5종 · 통합 플래너 구성
- Google Maps 지도, 도시 단위 클러스터, 장소 상세
- 날짜 범위 기반 코스 자동 생성, 숙박 추천
- KTX · SRT · 고속버스 · 렌터카 교통 연결
- 단일 파일 배포본 생성
