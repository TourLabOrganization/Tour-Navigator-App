# 변경 기록

날짜는 작업 시점 기준입니다.

## 2026-09-24

### 기능
- 내 플랜: 플래너와 테마 5종의 코스 탭에서 코스를 이름 붙여 저장 · 불러오기 · 삭제
- 배편 시간표: 제주 · 울릉 경로에 출항지 · 선사 · 소요시간 · 첫/막배와 공식 시간표 링크 (시간은 참고값, 선사 확인 필요)
- 테마 추천: 8문항 → 여행자 군집 8종 판정 → 테마 적합 점수로 3개 추천 (선호문항 구성안 반영)
- ME 화면: 추천받은 나의 테마와 전체 저장 플랜을 한 화면에 모음, 상단 홈 버튼

### 보안
- 화면 파일에 평문으로 있던 Google · YouTube · Kakao · 공공데이터 키 제거, `window.APP_CONFIG`(config.js)에서 읽도록 변경

### 정리
- 새 화면이 쓰지 않는 `shared.css` · `shared.js` 삭제

## 2026-09-23

### 스타일
- 7개 화면에 중복돼 있던 `<style>` 블록을 `shared.css` 로 추출 (09-24 화면 교체로 되돌림)
- 홈 화면의 짝이 맞지 않던 `}` 제거

## 2026-09-21

### 저장소 정리
- API 키를 `config.js` 한 파일로 분리하고 `.gitignore` 처리 (`config.example.js` 템플릿 추가)
- 각 화면 상단에 주석 헤더 추가
- Apache-2.0 라이선스, 문서, CHANGELOG · ROADMAP 추가

## 2026-09-20

- 전국 장소 1,197곳 / 110여 개 도시, TourAPI 연동, 100선 · 무장애 배지, 수도권 전철 · 국내선 · 여객선 경로, 6개 언어

## 이전

- 홈 화면 · 테마 5종 · 통합 플래너 구성, Google Maps 지도, 코스 자동 생성, 교통 연결, 단일 파일 배포본
