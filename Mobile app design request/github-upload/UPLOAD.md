# 2026-09-26 — 저장소 반영용 정리본

이 폴더의 7개 화면을 저장소 루트에 그대로 덮어쓰면 됩니다. API 키는 모두 `window.APP_CONFIG`(config.js)에서 읽도록 바꿔 두었고, 플래너와 테마 5종은 `<helmet>` 첫 줄에서 `config.js`를 불러옵니다.

## 이번에 저장소로 올라갈 변경
- 광역교통 2단계 선택(01 광역교통 · 02 현지 이동), 수단별 출발·귀가지 필터, 관문 ↔ 수단 자동 연결
- 광역 이동 체인(출발지 → 환승 관문 → 도착 관문)과 시각표, 복수 경로 선택 팝업, 환승 관문 변경
- 지하철(도시철도) 호선 → 역 선택, 자가용 확인 카드, 제주 카페리(자가용 + 배)
- 일자별 장소 추가(당일 도시 목록 · 닫기), 코스빌더 하단 저장·덮어쓰기
- 업스트림 09-25 기능 이식: 장소 상세 미세먼지 줄(6개 화면), 채널 탭 TMDB 작품 카드(테마 4종)

## 올린 뒤 확인
- `shared.js` 에도 같은 이름의 함수가 있습니다. 화면에 이식한 사본과 겹치지 않도록, 화면이 `shared.js` 를 읽는 구조로 되돌리려면 각 화면의 "업스트림 shared.js(2026-09-25) 이식" 블록을 지우면 됩니다.
- 플래너의 "여행 기간 행사 · 축제" 목록은 아직 이식되지 않았습니다.
